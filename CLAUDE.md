# SwingMCP — Claude 작업 지침

## 코드 수정 규칙 (최우선)

코드의 동작·방향이 바뀌는 수정은 반드시 사용자 확인을 받은 후 실행한다.
설명만 하고 기다린다. 허락 없이 Edit/Write 하지 않는다.

## 파이프라인 설계 원칙

**필터 통과 여부와 무관하게 모든 종목을 전체 분석·기록한다.**
- 필터 탈락 종목은 탈락 코드만 `filter_failures`에 기록하고, `filtered_tickers`에서 제거하지 않는다.
- Step 3 정적 필터와 Step 4 실시간 필터 모두 동일하게 적용한다.
- 유일한 예외: `F4_DELISTING_RISK` (상장폐지 위험) 종목만 hard stop으로 완전 제외한다.